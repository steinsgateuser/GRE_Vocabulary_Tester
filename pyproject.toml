[project]
name = "vocab-tester"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[project.scripts]
vocab-tester = "vocab_tester:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
